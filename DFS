class Node:
    def __init__(self, name):
        self.name = name 
        # to store neighbors
        self.adjacency_list = []
        self.visited = False
        
# recursive way to do it 
# the operating system creates its on stack 
def dep_first_search(node):
    node.visited = True
    print(node.name)
    for i in node.adjacency_list:
        if not i.visited:
            dep_first_search(i)

def depth_first_search(start_node):
    # must use stack for this (unless using recursion)
    # LIFO structure
    stack = [start_node]

    # iterate until stack becomes empty
    while stack:
        # returns last item we inserted 
        actual_node = stack.pop()
        actual_node.visited = True
        print(actual_node.name)

        for i in actual_node.adjacency_list:
            # if the node has not been visisted 
            if i.visited == False:
                i.visited = True
                # insert the item in the stack 
                stack.append(i)


node1 = Node("A")
node2 = Node("B")
node3 = Node("C")
node4 = Node("D")
node5 = Node("E")

# set the neighbors of each node
node1.adjacency_list.append(node2)
node1.adjacency_list.append(node3)
node1.adjacency_list.append(node4)
node3.adjacency_list.append(node4)
node3.adjacency_list.append(node5)
node4.adjacency_list.append(node2)
node4.adjacency_list.append(node5)



depth_first_search(node1)
dep_first_search(node1)

